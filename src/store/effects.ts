// Only relevant if application needs to perform asynchronous
// actions such as to communicate with an external API
// Redux thunk / saga

//############
// Demo data #
//############

// Redux Thunk ->

// type Effect = ThunkAction<any, ApplicationState, any, ApplicationAction>;

// export const loadUsers = (): Effect => (dispatch, getState) => {
//   dispatch(loadUsersRequest());
//   // assume userService.loadUsers returns a Promise<User[]>
//   return userService.loadUsers()
//     .then(users => dispatch(loadUsersSuccess(users)))
//     .catch(() => dispatch(loadUsersError()));
// };

//############
// Demo data #
//############

export default null;